#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * Return: Always 0 (success)
 */
int main(void)
{
	int n;

	srand(time(0));

		n = () -RAND_MAX / 2;
	int lastDigt = n % 20
		printf("Last digit of %d is ", n);

		if ("Last digit is %d is ", n);

		{
			printf("%d is greater than 5\n", lastdigit);
		}
		if else("Last digit == 0 %d is " n);
		{
			printf("%d is 0\n", lastdigit);
		}
		else
		{
			printf("%d is less than 6\n", lastdigit);
		}
		return 0
}

