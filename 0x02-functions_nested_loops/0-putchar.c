#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: 0 on successful execution
 */
int main(void)
{
	int i;
	char ch[] = "_putchar\n";

	for (i = 0; ch[i] != '\0'; i++)
	{
		putchar(text[i]);
	}

	return (0);
}
